numpy>=1.20.0
matplotlib>=3.5.0
yfinance>=0.2.0
pandas>=1.5.0
transformers>=4.30.0
# torch  # pip install torch (CPU version is sufficient)
# fredapi  # pip install fredapi (optional, for FRED macro data)
# vaderSentiment  # pip install vaderSentiment (fallback NLP if transformers fails)
